const validateFirstNameInput = () => {
  // do the stuff
};

const validateLastNameInput = () => {
  // do the stuff
};

const validateAgeInput = () => {
  // do the stuff
};

const validateGenderInput = () => {
  // do the stuff
};

export const validateSettingScreenInputs = () => {
  validateFirstNameInput();
  validateLastNameInput();
  validateAgeInput();
  validateGenderInput();
}